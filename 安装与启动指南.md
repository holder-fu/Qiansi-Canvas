# Qiansi-Canvas 安装与启动指南

本指南面向绿色运行包用户和源码开发者。画布核心、在线模型、可选 CLI 与本地 GPU 工具是彼此独立的能力：只想打开画布时，不需要先安装任何 AI 模型或 CLI。

## 1. 先判断你使用哪一种包

- 绿色运行包：已经包含预构建页面和匹配当前系统架构的 Node.js，直接使用系统入口，不需要 npm、TypeScript、Vite，也无需单独配置系统 PATH。
- 源码目录：需要先准备 Node.js 22.12.0+、安装依赖并完成构建。项目提供 Windows 与 macOS 一键安装入口。
- 更换电脑或 x64 / ARM64 架构后，请重新取得匹配架构的完整绿色包，不要复制另一架构的 `tools/runtime/node/`。

## 2. 安装前检查

1. 将整个目录解压到普通可写位置，不要直接在压缩包预览窗口中运行。
2. 路径中可以包含中文，但不要把程序放入需要管理员权限才能写入的系统目录。
3. 画布默认地址为 `http://127.0.0.1:2895`；若端口被其他 Qiansi-Canvas 或开发进程占用，请先关闭旧窗口。
4. 重要项目请自行备份。安装或修复只处理程序依赖，不应清理用户数据。

## 3. Windows 安装流程

### 绿色运行包

1. 双击根目录 `Qiansi-Canvas-windows.bat`。
2. 等待启动中心完成画布核心检查。
3. 点击“打开无限画布”。正常情况下会进入 `http://127.0.0.1:2895`。

### 源码目录的一键安装或修复

1. 双击 `tools/launchers/安装Qiansi-Canvas.bat`。
2. 未发现合格运行环境时，安装器会自动下载并校验项目专用 Node.js。
3. 下载来源是 Node.js 官方发布目录；安装器先读取 `SHASUMS256.txt`，核对压缩包 SHA-256 后才解压。
4. 项目专用运行环境放在 `tools/runtime/node/windows/`，依赖缓存放在 `tools/cache/npm/`。
5. 安装器随后安装源码依赖并执行构建验证。完成后使用根目录 `Qiansi-Canvas-windows.bat` 打开启动中心。

如果提示 2895 或 2896 端口仍在使用，请先关闭普通模式、开发模式及其终端，再重试。不要通过删除项目数据来处理端口问题。

## 4. macOS 安装流程

### 绿色运行包

首次运行前，在终端进入项目目录并执行：

```zsh
chmod +x Qiansi-Canvas-macOS.command
```

随后双击 `Qiansi-Canvas-macOS.command`。未签名包第一次打开时，可在 Finder 中按住 Control 点击文件，再选择“打开”。

### 源码目录的一键安装或修复

1. 运行 `tools/launchers/安装Qiansi-Canvas-macOS.command`。
2. 安装器会按 Apple Silicon 或 Intel 架构下载官方 Node.js，并使用 `SHASUMS256.txt` 校验。
3. 项目运行环境保存到 `tools/runtime/node/macos/`，npm 缓存保存到 `tools/cache/npm/`。
4. 安装完成后双击根目录 `Qiansi-Canvas-macOS.command`。

如果 `.command` 没有执行权限，可先运行：

```zsh
chmod +x tools/launchers/安装Qiansi-Canvas-macOS.command
chmod +x tools/launchers/安装CLI工具-macOS.command
```

## 5. Linux 安装流程

Linux 当前使用源码方式。需要 Node.js 22.12.0+ 与 npm：

```bash
npm ci --ignore-scripts --no-audit --no-fund
npm run build
export QIANSI_CANVAS_HOST='127.0.0.1'
node local-bridge.mjs
```

然后在浏览器打开 `http://127.0.0.1:2895`。

只在可信局域网需要其他设备访问时才启用 LAN 模式：

```bash
export QIANSI_CANVAS_LAN=1
export QIANSI_CANVAS_HOST='0.0.0.0'
node local-bridge.mjs
```

不要把 2895 端口直接暴露到公网。

## 6. 可选 CLI

CLI 不影响画布核心启动。需要时使用：

- Windows：`tools/launchers/安装CLI工具-Windows.bat`
- macOS：`tools/launchers/安装CLI工具-macOS.command`

安装器只负责安装并检测对应 CLI。账号登录、API Key、Cookie 和其他凭据由各服务自己的工具管理，不应提交到公开仓库。

## 7. 更新设置

“设置 → 系统更新”默认接入以下两个公开更新源：

- GitHub：`holder-fu/Qiansi-Canvas`
- 魔塔 ModelScope：`holder2895/Qiansi-Canvas`

更新中心只接受 HTTPS 清单和带 SHA-256 的完整版本包。上传源码并不等于发布可下载更新；维护者仍需在对应账号中发布 `update.json` 及其引用的版本包。程序不会把账号令牌写入源码或默认设置。

## 8. 数据目录与修复边界

- `data/` 保存项目、素材、插件、本机索引和运行设置。遇到启动问题时不要删除整个 `data/`。
- Node.js 运行环境损坏时，只处理对应的 `tools/runtime/node/windows/` 或 `tools/runtime/node/macos/`。
- 依赖下载异常时可检查 `tools/cache/npm/`，但清理缓存会导致下次重新下载。
- API Key、Cookie、CLI 登录令牌和个人素材不要提交到公开仓库。

## 9. 常见问题

### 页面能打开，但 CLI 显示不可用

先在新终端运行对应 CLI 的版本命令，再回到设置页点击“重新检测”。安装状态和账号登录状态相互独立。

### 启动中心提示端口被占用

关闭其他 Qiansi-Canvas、开发模式或占用 2895 / 2896 的程序后重新启动。不要同时运行普通模式与开发模式。

### 一键安装无法联网

确认系统时间、DNS、代理与防火墙允许访问 Node.js 官方发布站和 npm。校验或下载失败时安装器会停止，不会把未校验的 Node.js 压缩包当作成功结果。

## 10. 程序框架

Qiansi-Canvas 采用“浏览器界面 + 本机 Bridge + 可选外部能力”的分层结构：

1. `src/`：React、TypeScript 与 React Flow 组成的画布前端。节点、连线、指令框、素材库、设置页和多语言界面都在这一层。
2. `local-bridge.mjs` 及根目录服务模块：只在本机提供项目持久化、受管媒体、生成任务转发、更新检查和插件宿主接口。Bridge 默认仅监听回环地址。
3. `data/`：运行时数据根目录。项目、素材、插件和设置均与程序源码分离；公开源码包不会携带用户数据或已安装插件。
4. `public/` 与 `src/assets/`：前端需要的静态资源、轻量模型和界面素材。它们会在构建时进入 Web 产物。
5. `scripts/`：构建、测试、发布、完整性校验和安全门禁脚本。
6. `tools/launchers/`：Windows 与 macOS 的安装、修复和可选 CLI 入口。下载的运行环境与缓存不会提交到仓库。
7. `docs/`：更新清单、插件 SDK、兼容性和维护说明。

典型运行链路如下：浏览器界面向本机 Bridge 发起受限请求；Bridge 校验来源、权限、输入大小和任务并发后，再访问本机存储、用户配置的在线服务、CLI 或已安装插件；结果先由 Bridge 校验并持久化，再以受管地址返回画布。API Key 和登录令牌不进入画布文档，也不应写入源码。

Qiansi Audio Studio 是独立程序和独立仓库。画布仓库只保留通用插件宿主与受管安装接口，不内置其大模型权重、Python 环境或用户已安装的 Audio 插件目录；没有安装完整插件与模型时，画布不应把其工作台当作可运行功能展示。
