# Prompt Library public-source seed

`library.json` and `thumbnails/` may contain locally sampled or user-authored media. They are intentionally ignored and must not be copied into a public source release unless every item has explicit redistribution rights.

Public source staging copies `library.opensource.json` to `library.json`. This empty seed keeps the Prompt Library functional for user-created and imported records without redistributing local media frames.
