/**
 * Host-side compatibility contract for the optional Motion Captur plugin.
 *
 * The plugin is an independent release unit and is intentionally absent from
 * the public Canvas source package.  Keep the host contract here so the core
 * application can still type-check and build without the plugin installed.
 */
export const PLUGIN_STUDIO_MANNEQUIN_ID = 'qiansi-motion-capture.mannequin';
